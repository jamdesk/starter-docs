---
title: Code Blocks
description: Syntax highlighting and multi-language examples
---

Fenced code blocks get automatic syntax highlighting.

## Basic

```javascript
const acme = new Acme('sk_live_abc123');

const payment = await acme.payments.create({
  amount: 2500,
  currency: 'usd',
});
```

Add a language after the opening fence:

````mdx
```javascript
const acme = new Acme('sk_live_abc123');
```
````

## With filename

```javascript server.js
import express from 'express';
import { Acme } from 'acme-sdk';

const app = express();
const acme = new Acme(process.env.ACME_API_KEY);

app.post('/charge', async (req, res) => {
  const payment = await acme.payments.create({
    amount: req.body.amount,
    currency: 'usd',
  });
  res.json(payment);
});

app.listen(3000);
```

````mdx
```javascript server.js
import express from 'express';
// ...
```
````

## Code groups

Show the same thing in multiple languages:

<CodeGroup>
```javascript Node.js
const payment = await acme.payments.create({
  amount: 2500,
  currency: 'usd',
});
```

```python Python
payment = acme.payments.create(
    amount=2500,
    currency='usd',
)
```

```bash cURL
curl https://api.acme.com/v1/payments \
  -H "Authorization: Bearer sk_live_abc123" \
  -d amount=2500 \
  -d currency=usd
```
</CodeGroup>

````mdx
<CodeGroup>
```javascript Node.js
const payment = await acme.payments.create({ ... });
```

```python Python
payment = acme.payments.create(...)
```
</CodeGroup>
````
